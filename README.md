# ovena-backend

# integrate the hotel views
# add guest users. anonymos user ## shelfed for now
# apple users should die all of them, research it.
# refunds logic and how it should work
# split the endpoint to make easier, # update + email?? ✅
# bulk update / create for the menu reg system
# first in order split the json into section all the branches and all the categories and etc one by one bulk create each that way the is faster than a for loop-> 
# we can use batch updates for drivers locations:
## that way the location is updated at interval we store on every websocket interval and store periodically

# opt verfication; verifation mail for when updating the email
# register bag -> the bag for holding the food does that mean eac bag has a unique identifier
# car, motor cycle, bycyle, walking, keke -> vehicle type added to driver's module ✅
# passwords drivers -> login
# how lon is the session.

# oath for apple
# we don't reset the data after use like the drivers data like currentily available
# Race condition risk
# rate drivers and branches ✅
# -> can you rate two people (driver and resturant at the same time) ✅
# favorite system
# inputing menus
# refferal system, points and criteria
# can drivers be refered and added
# subscriptions
# search ✅
# first view ? ✅
# Debouncing for search and rate limits
# order code
# what happens if the driver rejects the request fail resistance design

# driver authentiction 

# improve oath

# endpoint for phone verification opt for oath users;
# first we want to use the users location to fiter for close resturants/ branches, now each of those branches should be shown with there menus, do we show with the avalibility as attached or not?

# global categories

# coupons use is to complete also i need a demo for the use of the coupons and the system for creating coupons. ✅
<!-- # do we store coupons in the user, etc?? -->

# is menu items variants reqired and the reuird addons 

# a big thing is how we will handle payments like final distribution
# refunds etc

i need an overview current system particularlly the menu system like it should be a mark down file that explains the stucture of the menu system including the payload of the menu creation put it in the menu system it self.


<!-- use cookie cutter generate a Django project and transfer all the necessary tools -->

drivers have passwords


work on the other views particulally the standalones
remove stand alone ensure profile

will be checking the notifiaction system and how it pairs with the websockets in the resturant how the entire sytem is digested for the drivers.

Cu la ticketj needas al usas websocketjn au another system mi hearis about system en firebase au en google